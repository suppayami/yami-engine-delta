/**
 * Create a ROT13-encoded version of the string. Added by the `foo` package.
 * @function external:Game_Actor#testFunc
 */

Game_Actor.prototype.hospitalFee = function() {
    var lostHp = Math.max(this.mhp - this.hp, 0),
        lostMp = Math.max(this.mmp - this.mp, 0),
        fee    = 0;

    // fee = lostHp * this.
};